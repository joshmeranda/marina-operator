apiVersion: terminal.marina.io/v1
kind: Terminal
metadata:
  labels:
    app.kubernetes.io/name: terminal
    app.kubernetes.io/instance: terminal-sample
    app.kubernetes.io/part-of: marina-operator
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/created-by: marina-operator
  name: terminal-sample
spec:
  # TODO(user): Add fields here
